mem1 = 10

mem2 = 15

regax = 0

regbx = 0

regcx = 0

regex = mem1/2

for i in range(regex):
    # regbx to find reminder
    regbx = mem1 - 2(regex)
    regex = regex -1
    # if jnz
    if regex == 0: